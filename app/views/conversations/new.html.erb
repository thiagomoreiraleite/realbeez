<div class="container">
  <h3 class="text-center my-5">Nouveau message</h3>
  <!-- ============================= -->
  <!--   <%#= form_tag conversations_path, method: :post do %>
    <div>
      <%#= select_tag :user_id, options_from_collection_for_select(User.all, :id, :prénom) %>
      <%#= select_tag :user_id, options_from_collection_for_select(@recipients, :id, :prénom) %>
      <%#= text_field_tag :user_id, input_html: {value: @recipient.id} %>
    </div>
    <div>
      <#%= text_field_tag :subject, nil, placeholder: "Objet" %>
    </div>
    <div>
      <%#= text_field_tag :body, nil, placeholder: "Votre message" %>
    </div>
    <%= submit_tag %>
  <%# end %> -->
  <!-- ========================= -->
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <%= form_for "", url: conversations_path do |f| %>
        <div class="form-group">
          <%#= f.select :user_id, (@recipients), rows: 4, class: "form-control" %>
          <%= f.select :user_id, options_for_select(@recipients.collect { | user | [user.prénom.capitalize, user.id] }), rows: 4, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_field :subject, required: true, placeholder: "Objet", rows: 4, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_area :body, required: true, placeholder: "Votre message", rows: 4, class: "form-control" %>
        </div>
        <%= f.submit "Envoyé", class: 'btn btn-yellow-mustard pull-right' %>
      <% end %>
    </div>
  </div>
</div>