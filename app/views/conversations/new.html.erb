<div class="container">
  <h3 class="text-center my-5">Nouveau message</h3>
  <!-- ========================= -->
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <%= form_for :conversation, url: conversations_path do |f| %>
        <div class="form-group">
          <%#= f.select :user_id, (@recipients), rows: 4, class: "form-control" %>
          <%= f.select :user_id, options_for_select(@recipients.collect { | user | [user.prénom, user.id] }), rows: 4, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_field :subject, placeholder: "Objet", rows: 4, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_area :body, placeholder: "Votre message", rows: 4, class: "form-control" %>
        </div>
        <%= f.submit "Envoyé", class: 'btn btn-yellow-mustard pull-right' %>
      <% end %>
    </div>
  </div>
</div>